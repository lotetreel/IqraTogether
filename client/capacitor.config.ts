import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.github.lotetreel.iqratogether',
  appName: 'IqraTogether',
  webDir: 'build'
};

export default config;
